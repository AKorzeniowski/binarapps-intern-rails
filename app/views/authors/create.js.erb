<% if @author.persisted? %>
  $('.authors').append('<%= escape_javascript render('author', author: @author) %>');
<% else %>  
  setTimeout(() => {
  	alert('Ogaaarnij się!');
  	alert('<%= j @author.errors.full_messages.join(', ') %>');
  }, 1000);
<% end %>
$('.new_author').find('input[type="text"], input[type="number"]').val('')