<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<hr>
	<%= f.label :content %>
	<%= f.text_area :content %>
	<hr>
	<%= f.label :author_id %>
	<%= f.text_field :author_id %>
	<hr>
	<%= f.submit 'Save' %>
<% end %>