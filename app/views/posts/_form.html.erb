<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<hr>
	<%= f.label :content %>
	<%= f.text_area :content %>
	<hr>
	<%= f.label "Author" %>
	<%= f.collection_select(:author_id, Author.all, :id, :fullname) %>
	<hr>
	<%= f.submit 'Save' %>
<% end %>